# Middleware

Request/response middleware for the backend API.

## Purpose
Middleware components that process requests before they reach endpoints and responses before they're sent to clients.

## What Belongs Here
- Authentication middleware
- Logging middleware
- Error handling middleware
- CORS configuration
- Request validation

## What Does NOT Belong Here
- Business logic (use services/)
- Route definitions (use api/routes/)
